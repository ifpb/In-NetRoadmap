name: inetrm
services:
  # Environment images
  basic:
    build: ./images/basic
    container_name: basic
    command: /bin/bash

  server:
    build: ./images/server
    container_name: srv
    cpus: '0.8'

  bmv2:
    build: ./images/bmv2
    container_name: bmv2
    command: /bin/bash

  client:
    build: ./images/client
    container_name: cli

    command: /bin/bash
    tty: true
    stdin_open: true

  # Containernet privileged image
  containernet-lab:
    image: containernet/containernet:latest
    container_name: containernet-lab
    privileged: true
    pid: host
    tty: true
    stdin_open: true

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /tmp:/tmp
      - ${PWD}/containernet:/scripts

    environment:
      - PYTHONUNBUFFERED=1

    working_dir: /scripts

    entrypoint: /bin/bash

    networks:
      - lab-network

    healthcheck:
      test: ["CMD", "docker", "ps"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  lab-network:
    driver: bridge
